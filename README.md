# CRM_Nice

Сучасний Django проект з мінімалістичним стеком для оптимальної продуктивності.

## 📦 Стек проекту

- **Django** - backend framework
- **normalize.css v12.1.1** - CSS нормалізація (недоторканний)
- **HTMX 2.0.8** - інтерактивність без JavaScript
- **Vanilla CSS** - стилізація з CSS custom properties
- **Vanilla JS** - мінімальна кількість JavaScript

## 🌐 Підтримувані браузери

- Chrome 90+
- Firefox 88+
- Safari 14+
- Edge 90+

(Браузери 2021 року та новіші)

## 📁 Структура проекту

```
CRM_Nice/
├── static/
│   └── css/
│       ├── normalize.css          ⚠️ НЕДОТОРКАННИЙ
│       ├── base.css               ✅ CSS variables
│       ├── components/            📁 Компоненти (BEM)
│       └── utilities/             📁 Утиліти
├── templates/
│   └── base.html                  ✅ Базовий шаблон
├── myapp/                         📱 Django app
├── .cursorrules                   ✅ Правила для Cursor AI
├── .gitattributes                 ✅ Захист normalize.css
├── CSS_STRUCTURE.md               📖 Документація CSS
├── HTMX_USAGE.md                  📖 Документація HTMX
├── README.md                      📖 Цей файл
└── QUICK_START.txt                📄 Швидкий старт
```

## ⚡ Критичний порядок підключення

В `templates/base.html` порядок має бути:

1. preconnect (CDN оптимізація)
2. normalize.css (ЗАВЖДИ ПЕРШИЙ)
3. base.css
4. components/*.css
5. utilities/*.css
6. CSRF token meta tag
7. HTMX (з defer)
8. Власні JS (з defer)

## 🔧 Налаштування Django

В `settings.py` вже налаштовано:

```python
STATIC_URL = '/static/'
STATICFILES_DIRS = [BASE_DIR / 'static']
STATIC_ROOT = BASE_DIR / 'staticfiles'
STATICFILES_STORAGE = 'django.contrib.staticfiles.storage.ManifestStaticFilesStorage'
```

## 🔗 Посилання на бібліотеки

### normalize.css
- Офіційний сайт: https://csstools.github.io/normalize.css/
- GitHub: https://github.com/csstools/normalize.css
- Версія: v12.1.1
- CDN: https://cdn.jsdelivr.net/npm/@csstools/normalize.css

### HTMX
- Офіційний сайт: https://htmx.org/
- Документація: https://htmx.org/docs/
- Приклади: https://htmx.org/examples/
- CDN: https://cdn.jsdelivr.net/npm/htmx.org@2.0.8/dist/htmx.min.js
- Django інтеграція: https://django-htmx.readthedocs.io/

## 📖 Документація

- **CSS_STRUCTURE.md** - Структура CSS, normalize.css, BEM, правила
- **HTMX_USAGE.md** - Приклади HTMX з Django, CSRF, компоненти
- **QUICK_START.txt** - Швидкий старт проекту

## ⚠️ Критичні правила

### ❌ НІКОЛИ:
- Не змінювати `static/css/normalize.css`
- Не використовувати inline styles (`style=""`)
- Не використовувати `!important` для перевизначення normalize
- Не порушувати порядок підключення CSS
- Не додавати jQuery, core-js, Bootstrap JS

### ✅ ЗАВЖДИ:
- Використовувати BEM методологію для класів
- Використовувати CSS custom properties (в base.css)
- Використовувати CSRF token для HTMX форм
- Підключати скрипти з `defer`

## 🚀 Швидкий старт

1. Переконайтеся, що структура створена
2. Налаштування в `settings.py` вже додано
3. Створіть Django app: `python manage.py startapp myapp` (вже створено)
4. Використовуйте `templates/base.html` як базовий шаблон
5. Додавайте стилі в `base.css` або `components/*.css`
6. Використовуйте HTMX для інтерактивності (див. HTMX_USAGE.md)
7. Запуск: `python manage.py runserver`

## 📊 Очікувана продуктивність

- **Lighthouse Performance**: 95+
- **First Contentful Paint**: <1s
- **Total Blocking Time**: <100ms
- **Cumulative Layout Shift**: <0.1
- **Загальний розмір**: ~65KB (normalize + HTMX + base.css)

## 🎯 Приклад використання

В вашому шаблоні:

```html
{% extends 'base.html' %}

{% block title %}Моя сторінка{% endblock %}

{% block content %}
    <h1>Привіт, світ!</h1>
    
    <!-- HTMX приклад -->
    <button hx-get="/api/data" hx-target="#result">
        Завантажити дані
    </button>
    <div id="result"></div>
{% endblock %}
```

## ✅ Готово до розробки!

Проект налаштовано для:
- ✅ 100% крос-платформенності (normalize.css v12.1.1)
- ✅ Оптимальної продуктивності (мінімальний стек, defer)
- ✅ Безпомилкової розробки (жорсткі правила в .cursorrules)
- ✅ Простої підтримки (чітка структура, документація)

Cursor AI автоматично дотримується всіх правил з `.cursorrules`.

Просто починайте розробку - все вже налаштовано!







