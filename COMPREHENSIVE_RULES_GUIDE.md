# Повний посібник правил кросплатформної розробки (Ultimate Edition 2025)

Цей документ містить **повний перелік всіх 110+ правил** з кросплатформних посібників та їх обґрунтування.

## 📋 Зміст

1. [Viewport та Safe Area](#viewport-та-safe-area)
2. [Макети: Flexbox, Grid, Positioning](#макети-flexbox-grid-positioning)
3. [Скролінг та Позиціювання](#скролінг-та-позиціювання)
4. [Форми та Ввід](#форми-та-ввід)
5. [Текст та Адаптивність](#текст-та-адаптивність)
6. [Медіа та Графіка](#медіа-та-графіка)
7. [Інтерактивність: Events та Gestures](#інтерактивність-events-та-gestures)
8. [Модальні вікна та UI](#модальні-вікна-та-ui)
9. [Advanced CSS](#advanced-css)
10. [JavaScript Правила](#javascript-правила)
11. [PWA та Офлайн](#pwa-та-офлайн)
12. [Reality Check - Приховані загрози](#reality-check---приховані-загрози)

---

## A. Viewport та Safe Area

### 1. Viewport meta з `viewport-fit=cover`

**Правило:** Viewport meta має містити `viewport-fit=cover`

**Обґрунтування:** Забезпечує коректне відображення на пристроях з вирізами (Notch, Dynamic Island)

**Приклад:**
```html
<meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover, interactive-widget=resizes-content">
```

### 2. Viewport meta з `interactive-widget=resizes-content`

**Правило:** Viewport meta має містити `interactive-widget=resizes-content`

**Обґрунтування:** Уніфікує поведінку viewport при появі клавіатури на Android та iOS

**Приклад:**
```html
<meta name="viewport" content="width=device-width, initial-scale=1.0, interactive-widget=resizes-content">
```

### 3. Body з `env(safe-area-inset-*)`

**Правило:** Body має використовувати `env(safe-area-inset-*)`

**Обґрунтування:** Забезпечує відступи для контенту на пристроях з вирізами

**Приклад:**
```css
body {
  padding-top: env(safe-area-inset-top);
  padding-bottom: env(safe-area-inset-bottom);
  padding-left: env(safe-area-inset-left);
  padding-right: env(safe-area-inset-right);
}
```

### 4. ❌ Заборонено `100vh` без fallback на `100dvh`

**Правило:** Заборонено використовувати `100vh` без fallback на `100dvh`

**Обґрунтування:** На мобільних браузерах `100vh` ігнорує адресний рядок, що призводить до стрибків контенту

**Приклад:**
```css
/* ❌ Погано */
.hero {
  height: 100vh;
}

/* ✅ Добре */
.hero {
  height: 100vh; /* Fallback */
  height: 100dvh;
}
```

### 5. Використання нових одиниць viewport

**Правило:** Використовуйте `100svh`, `100lvh`, `100dvh` замість `100vh`

**Обґрунтування:**
- `100svh` - висота видимого вікна (панелі розгорнуті)
- `100lvh` - максимальна висота (панелі сховані)
- `100dvh` - динамічна висота (адаптується в реальному часі)

---

## B. Макети: Flexbox, Grid, Positioning

### 7. ❌ `flex: 1` БЕЗ explicit `flex-basis` - ЗАБОРОНЕНО

**Правило:** Заборонено `flex: 1` без explicit `flex-basis`

**Обґрунтування:** Safari неправильно розраховує пропорції без явного `flex-basis`

**Приклад:**
```css
/* ❌ Погано */
.item {
  flex: 1;
}

/* ✅ Добре */
.item {
  flex: 1 0 0; /* або flex: 1 0 auto */
}
```

### 9. Grid gap: окремі властивості

**Правило:** Використовуйте `grid-row-gap` + `grid-column-gap` (не тільки `grid-gap`)

**Обґрунтування:** Старі версії Safari не підтримують скорочену властивість `grid-gap`

**Приклад:**
```css
.grid {
  grid-row-gap: 1rem;
  grid-column-gap: 1rem;
}
```

### 10. `position: sticky` з явними `top`/`bottom`

**Правило:** `position: sticky` потребує явних `top`/`bottom`

**Обґрунтування:** Без явних значень sticky не працює коректно

**Приклад:**
```css
.sticky-header {
  position: sticky;
  top: 0;
}
```

### 13. `overscroll-behavior: none` на `body`

**Правило:** `overscroll-behavior: none` має бути на `body` (не на `:root`)

**Обґрунтування:** В Chrome встановлення на `:root` не працює

**Приклад:**
```css
body {
  overscroll-behavior: none;
}
```

---

## C. Скролінг

### 18. Використання `scrollend` event

**Правило:** Використовуйте `scrollend` event замість таймерів

**Обґрунтування:** Більш точна подія завершення скролу

**Приклад:**
```javascript
element.addEventListener("scrollend", (event) => {
  // Логіка після завершення скролу
});
```

---

## D. Форми та Ввід

### 21. ❌ `<input type="tel">` БЕЗ `inputmode="tel"` - ЗАБОРОНЕНО

**Правило:** Заборонено `<input type="tel">` без `inputmode="tel"`

**Обґрунтування:** Критично для UX на мобільних - відкриває правильну клавіатуру

**Приклад:**
```html
<!-- ❌ Погано -->
<input type="tel" name="phone">

<!-- ✅ Добре -->
<input type="tel" name="phone" inputmode="tel">
```

### 22. ❌ `<input type="number">` БЕЗ `inputmode="decimal"` - ЗАБОРОНЕНО

**Правило:** Заборонено `<input type="number">` без `inputmode="decimal"`

**Обґрунтування:** На iOS `type="number"` відображає цифрову клавіатуру без крапки

**Приклад:**
```html
<input type="number" inputmode="decimal">
```

### 24. `accent-color` для checkbox/radio

**Правило:** Використовуйте `accent-color` для брендування нативних контролів

**Обґрунтування:** Підтримується сучасними браузерами, не потребує кастомізації

**Приклад:**
```css
input[type="checkbox"] {
  accent-color: var(--color-primary);
}
```

### 26. Autofill стилізація

**Правило:** Стилізуйте autofill через `input:-webkit-autofill`

**Обґрунтування:** Chrome та Safari додають жовтий фон до автозаповнених полів

**Приклад:**
```css
input:-webkit-autofill {
  -webkit-box-shadow: 0 0 0 30px white inset !important;
  -webkit-text-fill-color: black !important;
}
```

### 31. Мінімальний розмір полів `16px`

**Правило:** Мінімальний розмір полів `16px` для запобігання zoom на iOS

**Обґрунтування:** iOS автоматично збільшує сторінку при фокусі на полях менше 16px

**Приклад:**
```css
.form-control {
  font-size: 16px; /* Мінімум для iOS */
}
```

---

## E. Текст та Типографіка

### 33. ❌ `font-size` в `px` - ЗАБОРОНЕНО

**Правило:** Заборонено `font-size` в `px`, тільки `rem`

**Обґрунтування:** `rem` дозволяє браузеру коректно масштабувати інтерфейс при зміні системного розміру тексту

**Приклад:**
```css
/* ❌ Погано */
.text {
  font-size: 16px;
}

/* ✅ Добре */
.text {
  font-size: 1rem;
}
```

### 34. `text-wrap: balance` для заголовків

**Правило:** Використовуйте `text-wrap: balance` для `h1-h6` з fallback

**Обґрунтування:** Балансує рядки заголовків, уникаючи коротких останніх рядків

**Приклад:**
```css
h1, h2 {
  text-wrap: balance;
}
```

### 38. ❌ `line-height` для вертикального центрування - ЗАБОРОНЕНО

**Правило:** Заборонено використовувати `line-height` для вертикального центрування

**Обґрунтування:** Рендеринг шрифтів відрізняється на macOS та Windows

**Приклад:**
```css
/* ❌ Погано */
.icon {
  line-height: 32px;
}

/* ✅ Добре */
.icon {
  display: flex;
  align-items: center;
}
```

---

## F. Медіа: Зображення та Відео

### 41. `<picture>` з fallback

**Правило:** Використовуйте `<picture>` з `<source>` для AVIF/WebP/JPG fallback

**Обґрунтування:** Прогресивне вдосконалення - AVIF для сучасних, WebP для старіших, JPG як останній рубеж

**Приклад:**
```html
<picture>
  <source srcset="img.avif" type="image/avif">
  <source srcset="img.webp" type="image/webp">
  <img src="img.jpg" alt="..." loading="lazy">
</picture>
```

### 43-45. ❌ `<video>` атрибути - ЗАБОРОНЕНО

**Правило:** Заборонено `<video autoplay>` без `muted`, `playsinline` та `poster`

**Обґрунтування:**
- `muted` - необхідний для autoplay
- `playsinline` - необхідний для iOS
- `poster` - fallback зображення якщо відео не завантажиться

**Приклад:**
```html
<!-- ❌ Погано -->
<video autoplay src="video.mp4"></video>

<!-- ✅ Добре -->
<video autoplay muted playsinline loop poster="poster.jpg" src="video.mp4"></video>
```

---

## G. Інтерактивність: Events та Gestures

### 48. `touch-action: manipulation` для кнопок

**Правило:** Використовуйте `touch-action: manipulation` для кнопок

**Обґрунтування:** Усуває 300ms затримку та відключає подвійний тап

**Приклад:**
```css
button {
  touch-action: manipulation;
}
```

### 50-51. `pageshow` event для bfcache

**Правило:** Обов'язково слухайте `pageshow` event з перевіркою `event.persisted`

**Обґрунтування:** Safari відновлює сторінку зі знімка пам'яті при натисканні "Назад", JS не перезапускається

**Приклад:**
```javascript
window.addEventListener('pageshow', (event) => {
  if (event.persisted) {
    // Примусово знімаємо лоадери або активуємо кнопки
    document.body.classList.remove('is-loading');
  }
});
```

### 54. ❌ `:hover` БЕЗ `@media (hover: hover)` - ЗАБОРОНЕНО

**Правило:** Заборонено `:hover` без `@media (hover: hover)`

**Обґрунтування:** На тач-скрінах кнопка залишається "натиснутою" після тапу

**Приклад:**
```css
/* ❌ Погано */
.btn:hover {
  background: blue;
}

/* ✅ Добре */
@media (hover: hover) {
  .btn:hover {
    background: blue;
  }
}
```

---

## H. Модальні вікна та UI

### 56. Використання `<dialog>`

**Правило:** Використовуйте `<dialog>` замість custom modals де можливо

**Обґрунтування:** Нативний елемент з вбудованими засобами доступності та Top Layer API

**Приклад:**
```html
<dialog id="modal">
  <p>Контент модалки</p>
  <button onclick="document.getElementById('modal').close()">Закрити</button>
</dialog>
```

### 57. Використання `popover` атрибуту

**Правило:** Використовуйте `popover` атрибут для спливаючих елементів

**Обґрунтування:** Не блокує фон, працює в Top Layer API

**Приклад:**
```html
<button popovertarget="my-menu">Menu</button>
<div id="my-menu" popover>Content</div>
```

---

## I. Advanced CSS

### 63. `:has()` з fallback

**Правило:** Використовуйте `:has()` з fallback та `@supports selector(:has(*))`

**Обґрунтування:** Підтримується не всіма браузерами, потрібен fallback

**Приклад:**
```css
@supports selector(:has(*)) {
  .card:has(img) {
    border: 2px solid blue;
  }
}
```

### 64. `@layer` для керування каскадом

**Правило:** Використовуйте `@layer` для структурованого підключення стилів

**Обґрунтування:** Дозволяє впорядковувати групи стилів та уникати специфічності

**Приклад:**
```css
@layer reset, components, utilities;

@layer reset {
  *, *::before, *::after { margin: 0; padding: 0; }
}

@layer components {
  .btn { /* ... */ }
}
```

---

## J. JavaScript Правила

### 71. ❌ `var` - ЗАБОРОНЕНО

**Правило:** Заборонено `var`, тільки `const`/`let`

**Обґрунтування:** `var` має function scope, що призводить до проблем

**Приклад:**
```javascript
// ❌ Погано
var x = 1;

// ✅ Добре
const x = 1;
```

### 75. ❌ Inline scripts - ЗАБОРОНЕНО

**Правило:** Заборонено inline scripts (`<script>` без `src`)

**Обґрунтування:** Порушує Content Security Policy та ускладнює кешування

**Приклад:**
```html
<!-- ❌ Погано -->
<script>
  console.log('test');
</script>

<!-- ✅ Добре -->
<script src="main.js" defer></script>
```

### 77. `'use strict'` в IIFE

**Правило:** Використовуйте `'use strict'` в IIFE або ES modules

**Обґрунтування:** Запобігає помилкам та покращує продуктивність

**Приклад:**
```javascript
(function () {
  'use strict';
  // код
})();
```

---

## K. PWA та Офлайн

### 84. Service Worker тільки для кешування

**Правило:** На iOS використовуйте Service Worker тільки для кешування

**Обґрунтування:** iOS накладає обмеження - фонові синхронізації не підтримуються

---

## L. Стилі та БЕМ

### 89. ❌ `!important` - ЗАБОРОНЕНО

**Правило:** Заборонено `!important` (крім normalize.css override)

**Обґрунтування:** Порушує каскад та ускладнює підтримку

### 90. ❌ Inline styles - ЗАБОРОНЕНО

**Правило:** Заборонено inline styles (`style=""`)

**Обґрунтування:** Порушує розділення відповідальностей та ускладнює підтримку

---

## M. Reality Check - Приховані загрози

### 96. bfcache: `pageshow` event

**Правило:** Обов'язково слухайте `pageshow` event з `event.persisted`

**Обґрунтування:** Safari відновлює сторінку зі знімка пам'яті, JS не перезапускається

### 98. Липкий hover: `@media (hover: hover)`

**Правило:** Огортайте ховери в `@media (hover: hover)`

**Обґрунтування:** На тач-скрінах кнопка залишається "натиснутою" після тапу

### 99. Stacking context: `isolation: isolate`

**Правило:** Використовуйте `isolation: isolate` на контейнерах

**Обґрунтування:** 3D-трансформації у Safari створюють новий контекст накладання

### 100. Розширення (AdBlock): `min-height` замість `height`

**Правило:** Ніколи не використовуйте фіксовану `height` для блоків з текстом

**Обґрунтування:** Розширення можуть вбудовувати свій DOM, ламаючи верстку

---

## N. Django Специфічні

### 103. ❌ Розрив Django тегів - ЗАБОРОНЕНО

**Правило:** Заборонено розривати Django теги `{{ }}` та `{% %}` на кілька рядків

**Обґрунтування:** Призводить до небажаних пробілів у згенерованому HTML

**Приклад:**
```django
<!-- ❌ Погано -->
{{ company.name
    }}

<!-- ✅ Добре -->
{{ company.name }}
```

---

## Висновок

Дотримання цих правил гарантує:

1. ✅ Стабільну роботу на всіх платформах
2. ✅ Відсутність критичних багів
3. ✅ Якісний UX на мобільних пристроях
4. ✅ Доступність та сумісність

**Пам'ятайте:** Мета не "піксель-в-піксель ідентичність", а **UX-Consistency** - сайт має виглядати "рідним" для кожної платформи.

---

**Документ створено:** Грудень 2025  
**Версія:** Ultimate Edition 2025  
**Статус:** Актуально

